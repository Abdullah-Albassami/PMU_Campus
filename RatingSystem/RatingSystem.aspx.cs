using System;
using System.Web.UI;

namespace PMU_Campus.RatingSystem.RatingSystem
{
    public partial class RatingSystem : Page
    {
       /* protected void Page_Load(object sender, EventArgs e)
        {
            if (!IsPostBack)
            {
                advancedRating.Visible = false; // Hide advanced rating section on initial load
            }
        }

        protected void toggleAdvancedRating(object sender, EventArgs e)
        {
            advancedRating.Visible = advancedRatingCheckbox.Checked; // Toggle visibility based on checkbox
        }

        protected void SubmitRating(object sender, EventArgs e)
        {
            // Handle form submission logic here
            // Access the ratings using their IDs
            string overallRatingValue = overallRating.SelectedValue;
            string homeworkRatingValue = homeworkRating.SelectedValue;
            string examsRatingValue = examsRating.SelectedValue;
            string teachingRatingValue = teachingRating.SelectedValue;
            string gradingRatingValue = gradingRating.SelectedValue;
            string feedback = feedbackTextBox.Text;

            // Implement your data storage logic here
        }*/
    }
}
